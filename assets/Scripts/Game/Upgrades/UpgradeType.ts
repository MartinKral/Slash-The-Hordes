export enum UpgradeType {
    WeaponLength,
    WeaponDamage,
    HorizontalProjectile,
    HaloProjectlie,
    Regeneration
}

export enum MetaUpgradeType {
    MaxHp,
    OverallDamage,
    ProjectilePiercing
}
